# Not currently used — the base image (myoung34/github-runner) has jq,
# and actions/setup-node@v4 installs Node.js 20 at runtime.
#
# Keep this file for future customization (e.g., pre-installing Node.js
# once the base image updates to a non-EOL Ubuntu).
FROM myoung34/github-runner:latest
